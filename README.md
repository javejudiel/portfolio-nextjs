# Jave Judiel Portfolio

A fresh Next.js portfolio rebuild using the App Router, TypeScript, custom responsive CSS, and local portfolio data.

## Scripts

```bash
npm install
npm run dev
npm run build
```

The primary content lives in `data/portfolio.ts`.
